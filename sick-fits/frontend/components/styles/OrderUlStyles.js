import styled from "styled-components"

const OrderUl_Styled = styled.ul`
  display: grid;
  grid-gap: 4rem;
  grid-template-columns: repeat(auto-fit, minmax(40%, 1fr));
`;

export default OrderUl_Styled;
