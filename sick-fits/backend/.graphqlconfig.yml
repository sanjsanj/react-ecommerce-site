projects:
  app:
    schemaPath: "src/schema.graphql"
    includes: ["**/*.graphql"]
    extensions:
      endpoints:
        default: "http://localhost:4444"
  prisma:
    schemaPath: "src/generated/prisma.graphql"
    extensions:
      prisma: prisma.yml
